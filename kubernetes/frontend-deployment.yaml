apiVersion: apps/v1
kind: Deployment
metadata:
  name: library-frontend
  labels:
    app: library
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: library
      component: frontend
  template:
    metadata:
      labels:
        app: library
        component: frontend
    spec:
      containers:
      - name: frontend
        image: ghcr.io/your-username/library-management-system-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: library-frontend-service
  labels:
    app: library
    component: frontend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: library
    component: frontend
